name: Deploy

on:
  release:
    types: [published]

jobs:
  deploy-upm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14
          registry-url: https://upm.fenrirserver.org/

      - run: cd UnityPackage/Assets
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_TOKEN}}
